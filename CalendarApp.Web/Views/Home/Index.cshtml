@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">CalendarApp</h1>
    <p>Manage your personal calendar with events, notifications, and sharing.</p>
    <div class="mt-4">
        <a href="@Url.Action("Login", "Account")" class="btn btn-primary me-2">Login</a>
        <a href="@Url.Action("Register", "Account")" class="btn btn-secondary">Register</a>
    </div>
</div>
